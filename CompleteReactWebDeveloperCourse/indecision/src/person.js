
export const isAdult = (age) => age > 17 ? true: false;   // could have done age >= 18 so (age) => age >= 18

export const canDrink = (age) => age > 20 ? true: false;  // could have done age >=21 so (age) => age >= 21